# Shared Infra Boundary

This directory is a marker for future shared infrastructure ownership.

Shared runtime behavior should stay in the Python application and
infrastructure adapters until a reviewed workflow change defines committed
assets for this boundary.
