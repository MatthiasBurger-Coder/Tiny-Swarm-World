# Platform Boundary

This directory is a marker for future platform infrastructure ownership.

Current platform behavior is implemented through the Python setup workflow and
the LXC-native provider configuration under `infra/config`. Do not add
executable platform setup entry points here without a reviewed workflow change.
