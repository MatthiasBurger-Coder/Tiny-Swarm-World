# Retired Prepare Helpers

This area documents retired direct service preparation helpers. It must not
contain executable setup entry points.

Use the guarded setup workflow instead:

```sh
PYTHONPATH=src python3 -m tiny_swarm_world setup run --live
```

Legacy helper notes remain only to explain where former scripts moved from and
why direct `infra/prepare` execution is no longer supported.
