# Retired Prepare Helpers

This directory is retained as documentation space for former direct prepare
helpers. It must not contain executable setup entry points. Use the guarded
Python setup workflow:

```bash
PYTHONPATH=src python3 -m tiny_swarm_world setup run --live
```
