# Retired Portainer Prepare Helpers

Former Portainer direct helpers such as `prepare.sh` and `portain_setup.py` are
retired. Portainer bootstrap is owned by the Python setup workflow, including
admin initialization and local Docker endpoint registration.

Do not restore executable helpers in this directory without a reviewed workflow
change.
