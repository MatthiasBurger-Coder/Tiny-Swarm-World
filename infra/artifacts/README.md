# Artifacts Boundary

This directory is a marker for future artifact infrastructure ownership.

Current artifact behavior is implemented through the Python setup workflow and
the supported assets under `infra/compose`. Do not add executable bootstrap
entry points here without a reviewed workflow change.
