# Deployment Infrastructure Boundary

This directory marks the target home for stack and service deployment assets.

Current deployment assets remain in their existing locations for compatibility:

- `infra/config/compose`
- `infra/compose`
- `infra/prepare/portainer`

Do not move live stack deployment scripts into this directory without a
dedicated migration slice and compatibility handling.
