# Deployment Boundary

This directory is a marker for future deployment infrastructure ownership.

Current stack deployment behavior is implemented through the Python setup
workflow and the service stack configuration under `infra/config/compose`. Do
not add executable deployment entry points here without a reviewed workflow
change.
