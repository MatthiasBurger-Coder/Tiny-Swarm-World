# Slice 12 consolidation

The targeted Linux/host regression passed `202` tests and architecture
contracts prevent native Windows operations. The actual Ubuntu 24.04.4 native
Linux host-platform regression also passed detection, prepare, verify and
cleanup without selecting the Windows runner. Slice 12 is locally PASS.
