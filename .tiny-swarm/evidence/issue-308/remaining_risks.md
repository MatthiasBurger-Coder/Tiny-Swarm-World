# RC1-R07 Remaining Risks

No mapped documentation/journey requirement remains open. Host preparation remains
an explicit operator responsibility. The journey starts from an empty managed
target on already qualified hosts and may reuse immutable image/dependency caches;
it is not a bare-metal operating-system installation. Native/WSL timings and memory
snapshots are observations, not peak measurements or universal performance promises.

The qualified WSL restart is a planned distribution boundary with ordered Docker
shutdown; its first failed attempt and repair remain visible. Native evidence is a
separate actual VM reboot. Scoped security residuals remain R08/R06 inputs.

Real agents reached their usage limit. The permitted root-AGENTS sequential
requirement/architecture/QA fallback supplies the operator-oriented review, with
no newly obtained independent human or agent approval invented. R06 owns final
integrated release and cleanup verification.
