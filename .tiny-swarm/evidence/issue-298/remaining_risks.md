# RC1-R02 Remaining Risks

No native acceptance requirement remains open. Qualification applies to the
observed Ubuntu 26.04.1 x86_64 host, kernel and Incus version; it is not a claim
about every Linux distribution, hard power loss or arbitrary service-data upgrades.
The first shared-network name conflict and optional operator probe invocation
failure remain explicit. Source directory mode 0755 triggers the reused evidence-
directory diagnostic, while the actual source filesystem is native and the separate
secret/evidence guards pass 0600/0700; no permission check was weakened.

Native bedb fault/node observations are reused only for verified identical product
scope. Fresh install, full lifecycle and actual VM reboot execute c921 itself.
Cleanup preserves original and fresh volumes and restores original names/project;
the VM is shut down when no longer needed, as the user instructed.

Final review uses the root-AGENTS sequential requirement/architecture/QA fallback
after real agents reached their session limit. This is not a new independent
agent/human approval. R06 owns final integrated acceptance and cleanup reconciliation.
