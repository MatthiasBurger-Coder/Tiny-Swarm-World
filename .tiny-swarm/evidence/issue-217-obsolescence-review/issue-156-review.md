# Issue #156 Review — Central Published Ports

Decision: `KEEP_OPEN`

Review baseline: `main` at `ecdc71d94a72530905ecb0a41d2845921ad6debb`.
The issue body and acceptance criteria were re-read from GitHub before this
decision. No historical commit message was used as completion evidence.

## Requirement trace

| Check | Status | Current evidence |
|---|---|---|
| Direct published ports resolve from the central registry | `BLOCKED` | `infra/config/ports.yaml:51-337` and `PortRegistryYamlRepository` provide the registry, but `ComposeFileRepositoryYaml._DIRECT_PUBLISHED_PORT_IDS` does not include `service-access`; `infra/config/compose/service-access/docker-compose.yml:17-24` still publishes `10000` and `8086` directly. |
| Image-specific internal targets remain compatible | `VERIFIED_LOCAL` | Registry `internal_port` values and Compose target assertions remain covered; the Pulsar image uses internal `6650`, `8080` and `9527`. |
| Generated direct URLs and health checks use the effective published map | `BLOCKED` | Registry-backed ingress projection exists in `src/tiny_swarm_world/domain/ingress/desired_state.py:211-277`, but direct producers remain in `service_stack_contract.py:108-181`, `installer.py:62-72`, `composition.py:284,1571-1590` and `service-access/nginx/default.conf:14-75`. |
| Safe evidence contains the effective direct published-port map | `VERIFIED_LOCAL` | `.tiny-swarm-world/evidence/solid-typed-evidence/routing/effective-access-model.json` is produced by the redacted effective-access evidence service and contains allowlisted public and compatibility mappings. |
| Representative services and legacy messaging behavior are covered | `VERIFIED_LOCAL` | Compose and optional-routing tests cover Jenkins, Nexus, Pulsar, SonarQube, Swagger, Infisical, service-access, Traefik, Prometheus, Grafana and Tiny Swarm routing. Unsupported legacy messaging is not generated by the active path. |

## Current implementation and gaps

The implementation is materially present but not complete. Central registry
loading and the effective-access projection exist, while a direct publisher and
several URL/readiness producers still bypass the effective model. The issue's
central requirement therefore remains materially incomplete. A full repository
quality result is `UNVERIFIED` for this slice; the required full gate belongs to
the consolidation/final slices.

## Recommended issue action

Keep #156 open and add a current evidence comment. Keep its valid acceptance
criteria, but clarify the residual scope as: centrally resolve every direct
publisher including service-access, route direct URLs/readiness through the
effective published map, and add end-to-end propagation regression tests.

Closing reason: not applicable; the issue is not eligible for closure.

