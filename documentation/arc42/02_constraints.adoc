== Constraints

Tiny Swarm World is constrained by the repository safety and architecture
rules.

* The supported operator shells are native Linux and WSL2. WSL2 is a dedicated
  host platform, not ordinary native Linux. WSL1, non-Linux systems, and
  contradictory or unverifiable WSL signals fail closed for live setup.
  Documentation and examples use POSIX paths and commands.
* Host classification uses the single typed detector contract behind
  `PortHostEnvironmentDetector`. Domain/application code does not read `/proc`,
  environment variables, or host files directly, and legacy consumers must not
  independently infer WSL from one signal.
* Domain code must not import application or infrastructure modules.
* Application services depend on ports and domain objects, not concrete
  adapters.
* `src/tiny_swarm_world/infrastructure/composition.py` remains the composition
  root for concrete adapters.
* `src/tiny_swarm_world/__main__.py` remains a thin workflow entry point.
* Normal `platform init`, `platform reconcile`, and `platform expose` must not
  select destructive cleanup commands.
* `platform reset` and `platform destroy` require exact confirmation phrases.
  Their destructive execution is limited to configured managed LXC-native
  nodes or marker-verified project-owned state, and blocks before mutation
  when ownership evidence is missing or mismatched.
* Fresh-install reset scope is limited to Tiny Swarm World managed local
  resources. LXC-native reset may target configured provider nodes or
  marker-verified instances with `user.tiny_swarm_world.managed=true`; broad
  host/provider commands that delete every Incus container are
  forbidden.
* Mutating workflows require live-infrastructure consent before services are
  constructed.
* Command YAML under `infra/config/**/command_*.yaml` is product configuration
  and must carry typed safety metadata, allowed workflows, effects, and
  verification specifications.
* Desired inventory may be committed configuration. Observed inventory and
  verification evidence are local artifacts under `.tiny-swarm-world/` and
  must not be committed.
* Mutating workflow steps continue only after apply succeeds and verification
  returns `verified` evidence.
* The default quality gate must not create provider nodes or VMs, mutate
  networking, initialize Docker Swarm, deploy stacks, or bootstrap services.
* LXC-native through Incus is the supported provider direction.
* Raw low-level LXC command automation is not an accepted first default
  provider path.
* A failed Incus readiness check must not silently fall back to another
  provider.
* WSL2 is capability-gated for managed LXC provider behavior. WSL1 is not a
  supported provider target.
* Host package installation, daemon repair, group membership changes,
  privileged container profiles, bridge/firewall changes, and broad host
  mounts require explicit operator approval and may require later ADR coverage
  before becoming automated defaults.
* Non-interactive live consent is allowed only as explicit per-invocation
  `--live --approve-live` consent, as governed by
  `adr-explicit-non-interactive-live-consent.adoc`; headless or CI execution
  must never imply approval.
